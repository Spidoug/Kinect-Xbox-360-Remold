# Linux application binaries

The Processing application sources are Linux-aware, but a native Linux launcher bundle has not yet been exported into this repository. Use `applications/processing/` from Processing while this release payload is prepared.

This directory is intentionally separate from `windows-x64/` so Windows `.exe` launchers are never mistaken for Linux binaries.

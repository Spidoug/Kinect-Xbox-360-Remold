# Exported application binaries

Platform release payloads live here. Do not edit generated JAR/launcher files as the source of truth.

- `windows-x64/` — current exported SynKinect launchers and shared runtime libraries.
- `linux-x64/` — reserved for a future exported Linux application bundle; see its README for current status.
